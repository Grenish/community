import { useEffect } from "react";

const Feeds = () => {
  useEffect(() => {
    document.title = "Feeds";
  });

  return (
    <div>
      <h1 className="">Feeds</h1>
    </div>
  );
};

export default Feeds;
